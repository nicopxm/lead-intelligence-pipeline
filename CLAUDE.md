# CLAUDE.md — Lead Intelligence Pipeline (repo instructions for Claude Code)

## What this is
AI-powered lead pipeline: new lead → enriched → scored vs configurable ICP → personalized draft email → HubSpot, in <90 seconds. Portfolio anchor project for Wop (Full-Stack/Automation/RevOps/Growth Engineer roles). Phase 2 = deploy for a real business, so EVERYTHING is config-driven, never hardcoded.

## Your role vs the architect chat
You (Claude Code) do ALL implementation: code, terminals, servers, debugging, commits — one GitHub issue at a time. Design decisions, prompt engineering, sprint planning, and doc drafting happen in a separate architect chat with Wop. If a task requires a genuine design/architecture decision not covered by docs/, STOP and surface it to Wop — do not decide under momentum.

## Stack (decided — do not re-litigate)
Next.js/Vercel · Supabase (Postgres, source of truth) · n8n self-hosted Docker on Hetzner · HubSpot Free · Claude API · Resend

## Architecture (5 components)
1. INTAKE: Next.js form + universal webhook. Schema: {name, email, company, domain, source, message, timestamp}
2. ENRICHMENT (n8n): website scrape (home/about/pricing) w/ retries+timeouts+graceful partial failure; tech-stack detection; news via search API. Dedupe: same email/domain in 30 days = update not insert. Paid enrichment (Clearbit/Apollo) must be swap-in-able.
3. INTELLIGENCE (Claude API): one structured call → strict JSON {icp_score 0-100, score_reasoning, company_summary, buying_signals[], objection_risks[], draft_email}. ICP lives in config JSON (docs/ICP-CONFIG.md). Validate + retry malformed output. Log tokens/cost per lead.
4. CRM DELIVERY: HubSpot properties + note w/ draft email; score ≥75 → instant alert; ALL errors alert Wop. No silent failures, ever.
5. REPORTING: Next.js dashboard (volume, score distribution, source quality, latency); weekly n8n cron → Claude-written summary → Resend.

Full rationale: docs/ARCHITECTURE.md. Read it before structural work.

## Success metrics
<90s form-to-CRM · <$0.02/lead · >85% enrichment success · zero silent failures

## Working rules
- Session start: check LOG.md has a line for today's date. If missing, prompt Wop for Y/T/B (Yesterday/Today/Blockers) before starting any issue work — don't write the line yourself, LOG.md is Wop's.
- Work ONE GitHub issue at a time (single-piece flow). The issue's acceptance criteria are the spec. Reference the issue in commits; close with "Closes #N".
- Board (GitHub Projects: Backlog → Sprint → In Progress → Done) is the single source of truth. Move the issue to In Progress when starting.
- Mid-task ideas → create a Backlog issue via `gh issue create`, do NOT implement.
- Definition of Done: merged to main, pushed to origin, deployed, verified end-to-end, errors alert, docs updated, issue closed. Never end an issue with unpushed commits. If the issue touched n8n workflows, do a fresh export (n8n "..." menu → Download) of every affected workflow and diff it against what's committed in `n8n/workflows/` before closing — live in-editor fixes (credential wiring, node reconfiguration, drift from the originally-imported JSON) do not retroactively update the repo on their own. **Final step, every issue**: verify actual board state via `gh project item-list <n> --owner nicopxm --format json` (or the API) before declaring done — confirm (a) the closed issue's card shows Done, (b) any issue created mid-session (bugs found live, backlog spin-outs) is actually ON the board and in the correct column, (c) rolled/re-triaged issues' columns match their real status. If GitHub's automation already did this, the check takes 5 seconds; if not, fix it in the same session — don't defer to "next time."
- Config-driven everything: ICP, credentials, client identity, thresholds. If you're about to hardcode a value that could differ per tenant, put it in config/env instead.
- No silent failures: every workflow/route gets an error path that alerts. If you can't alert yet (early sprints), log loudly and leave a TODO issue.
- Legal data sources only. No LinkedIn scraping, ever.
- Cost discipline: free tiers, cheapest options, batch/cache Claude calls where possible.
- Monday planning updates the sprint milestone description with goal / scope notes / named day — the milestone is the planning record.
- Reading credentials from `.env`/`web/.env.local` to run verification (Supabase/HubSpot REST or API queries) is sanctioned standing policy, not a one-off — this is how sessions confirm real production state instead of asking Wop to relay it. Three rules govern it: (1) never echo secret values into output — shape/presence checks only (pattern-match a prefix, check non-empty), same method as the secrets rule below; (2) read-only verification calls (SELECTs, GETs, searches) are fine to run unprompted, no need to ask first; (3) any WRITE to a production system via a direct API call made *outside* a workflow's own execution — inserts, deletes, config changes — gets named explicitly in the session summary, the way #24's test-row/test-contact cleanup was called out. See docs/RUNBOOK.md's "Local credentials surface" note for the key-rotation consequence of this.

## Docs to maintain
- docs/RUNBOOK.md — update when infra changes (server setup, docker, env, restore steps)
- docs/DECISIONS.md — one line per significant choice (date, decision, why)
- .env.example — keep current with every new env var
- LOG.md — Wop's daily standup lines (don't edit)
- RETROS.md — Friday retros (don't edit)

## Conventions
- TypeScript everywhere in the Next.js app; zod for validation at every boundary (webhook payloads, Claude JSON output, ICP config load).
- Supabase migrations in repo (`supabase/migrations/`), never dashboard-only schema changes.
- n8n workflows exported as JSON into `n8n/workflows/` after every change — workflows must be restorable from the repo.
- Secrets only in env; .env.example documents every var with a comment.
- Commits: small, imperative mood, reference issue number.

## Current status
Sprint 1 — CLOSED. Goal (form submission lands in Supabase and HubSpot raw) met and verified.
Done: #1 (scaffolding), #2 (Hetzner + Docker + n8n), #3 (Supabase project + leads schema migration), #4 (HubSpot Service Key + contact verification — used a Service Key instead of the legacy private app named in the issue, see docs/DECISIONS.md), #7 (universal webhook intake — verified end-to-end incl. a forced-failure test of the dead-letter/alert path; several real n8n/Caddy bugs found and fixed along the way, see docs/DECISIONS.md and docs/RUNBOOK.md), #5 (Next.js TypeScript app scaffolded in web/, Vercel CI/CD connected — Root Directory=web and Framework Preset=Next.js both had to be set explicitly or every route 404'd; verified production deploy and PR preview deploy live, see docs/RUNBOOK.md), #6 (intake form at / — shared zod validation client+server, domain derivation with free-mail exclusion, posts via a server-side API route to the n8n webhook; verified end-to-end on production including the free-mail edge case and failure states preserving input, see docs/RUNBOOK.md), #17 (demo-ready form styling — CSS Modules + custom properties, no new deps, single accent color, focus/loading/success/error states, mobile-verified on production, see docs/RUNBOOK.md), #8 (Sprint 1 end-to-end verification — fresh happy-path run (Supabase row + HubSpot contact confirmed via API) and deliberate HubSpot-credential-failure test (dead-letter to status=error, no HubSpot contact, correct alert email, clean restore with no republish needed) both verified in production on 2026-07-08; HubSpot Free's default contacts list view doesn't reliably show custom properties — use a saved view, see docs/RUNBOOK.md; 60s review recording linked in the issue). Also: custom domain leads.nicopxm.me wired (revises the 2026-07-02 root-domain decision — root is now reserved for a future portfolio site, see docs/DECISIONS.md); the earlier stray duplicate Vercel project and its Safe Browsing flag are fully resolved (docs/RUNBOOK.md).
Backlog (not scheduled): #15 (replace Caddy basic auth with session-based access — spun out of #7), #18 (Form UI polish/branding/animations — spun out of #17, deferred to Sprint 5), #26 (Tech Stack Detector `config_unreadable` regression — config file present/readable but the node fails anyway, found live during #23; every real lead currently gets `tech_stack.status: "failed"` until this lands). None block anything.
Sprint 2 — CLOSED (2026-07-17). Goal (every lead automatically gains a structured company profile) met. #19/#20/#21/#22 done (see prior entries). #23 (enrichment orchestration: new `Enrichment Orchestrator` workflow composes Website Scraper → Tech Stack Detector → News RSS sequentially, writes `leads.status`/`enriched_at`/new `enrichment_duration_ms` column iff at least one component reached ok/partial, else leaves status at raw and alerts; wired into `Lead Intake` as a fire-and-forget branch off `Supabase - Insert Lead`, `waitForSubWorkflow: false` verified against the live n8n image so the ~45s enrichment budget never blocks the webhook response, measured 3.4s; live-verified 2026-07-17 end-to-end through the real webhook — not a harness, retiring the #21/#22 harness pattern — both happy path (Stripe lead → enriched, all Supabase fields populated, duration recorded) and alert path (unreachable domain + no company → stays raw, exactly one alert delivered per Resend's own send log); two real orchestration bugs found and fixed live — missing `onError` handling let one component's genuine failure kill the whole chain and double-alert, and missing `alwaysOutputData` let a sub-workflow's zero-item success silently truncate the chain (a recurrence of #21's "zero output items" gotcha) — both now in docs/RUNBOOK.md/DECISIONS.md; also surfaced and filed #26, a pre-existing Tech Stack Detector regression, unrelated to #23, not fixed here) done.
Sprint 3 — CLOSED (2026-07-23). Goal (every lead arrives scored, tiered, and with drafted outreach against the ICP config) met. #26 closed 2026-07-20: root cause was n8n's fire-and-forget (`waitForSubWorkflow:false`) sub-workflow invocation breaking an async binary-data helper call several levels down the `Lead Intake` → `Enrichment Orchestrator` → `Tech Stack Detector` chain — reproducible only through the real webhook path, never in standalone or manual full-chain testing (see docs/RUNBOOK.md's n8n operational notes). Fixed by replacing the Code-node `getBinaryDataBuffer` call with n8n's built-in Extract From File node; re-verified #21's full original acceptance criteria (3+ known-stack sites, no-HTML skip, config-unreadable failure+alert, clean recovery) through 6 real leads via the actual webhook, not editor tests. Also added a RUNBOOK "Verification schedule" section per the Sprint 2 retro's re-verification-cadence takeaway. #24 closed 2026-07-21: dedupe enforcement (30-day update-not-insert) at intake — matching resolved as domain-priority-with-email-fallback (not a literal "email OR domain" reading, which contradicted the issue's own job-change bullet and ARCHITECTURE #6; documented on the issue and in docs/DECISIONS.md), `Lead Intake` gained a `Compute Dedupe Key → Has Domain? → Find Lead By Domain/Email → Decide Dedupe Action → Insert or Update? → Normalize Lead Record → Should Reenrich?` branch, HubSpot's contact node turned out to already upsert by email (rename only, no logic change), and the job-change edge case's Supabase-vs-HubSpot asymmetry (two leads, one contact) was deliberately accepted and live-verified, not left emergent — see docs/DECISIONS.md and docs/RUNBOOK.md's new "Dedupe enforcement (#24)" section for full verification evidence. #25 remains absorbed into Sprint 3 verification per the milestone description. #28 closed 2026-07-21: ICP config decided and shipped as `config/icp.default.json` (tenant `flowsignal`, 5 weighted scoring dimensions, hard disqualifiers, thresholds, email rules — all v1 hypotheses pending #30's tuning, see docs/DECISIONS.md), loaded via the new `ICP Config Loader` sub-workflow (validates on load, fails loud + alerts on a bad config, never falls back to a silent default); verified live via 4 forced invalid-config cases plus a valid-config pass, all confirmed through Resend's send log; also found and fixed the VPS repo clone being stale since ~#21 (nothing auto-syncs it from GitHub — `git pull` on the VPS is now a documented required first step before any deploy touching a repo-mounted file). #29 closed 2026-07-22: `Intelligence Scorer` sub-workflow built — one structured `claude-haiku-4-5` call per lead (prompt in `prompts/lead-scoring.v1.md`, approved verbatim) returning per-dimension scores+evidence, `buying_signals[]`, `objection_risks[]`, `company_summary`, `draft_email`; CODE (not the model) computes the weighted `icp_score`, applies the disqualifier cap, assigns `tier`, and — since the approved one-call design means the model always drafts — discards `draft_email` before persisting when `tier === "discard"` (nurture gate governs delivery, not generation; reconciles a stale AC line, see docs/DECISIONS.md 2026-07-22); malformed/invalid model output retries once then dead-letters to `status=error` + alert; wired into `Enrichment Orchestrator` right after `Supabase - Mark Enriched`. Live-verified through the real webhook with 16 real leads (happy path scored `hot` at $0.007/lead — the first real unit-economics data point, well under the $0.02 target; disqualifier cap confirmed engaging on a weighted total of 42.5 → clamped to 10; nurture tier confirmed retaining its draft; a forced malformed-response test confirmed retry → dead-letter → Resend-delivered alert → clean recovery), all test data cleaned up after — see docs/RUNBOOK.md's "Intelligence Scorer (#29)" section. Surfaced, filed, not fixed: #32 (`Lead Intake` dead-letters leads whose domain uses a reserved/special-use TLD like `.test`/`.example`, before enrichment even runs). #30 closed 2026-07-23: verification complete, but honestly mixed — the null-domain/dedupe/prompt-drift/cost-report ACs were verified live, while the human tier-agreement spot-check is recorded as `[~] ATTEMPTED, DEFERRED to Sprint 4`, not satisfied: three spot-check batches (33 test leads) were run, but each was unusable for its intended purpose for a different reason (batch 1's real recognizable SaaS brands tripped a competitor-detection bug with zero tier variance; batch 2's scores were seen before judging, anchoring the blind protocol; batch 3 was run genuinely blind but turned out to be off-ICP under a scope decision made mid-verification, and 7 of its 12 leads were disqualifier-gate-capped before reaching real weighted scoring) — see docs/DECISIONS.md's 2026-07-23 entries and #30's closing comment for the full evidence chain. Four bugs surfaced, filed, not fixed: #34 (`Enrichment Orchestrator`'s `anyOk` gate conflates `skipped` with `failed`, silently dropping all-skip leads from scoring), #35 (disqualifier drift — obvious-competitor over-triggers on recognizable brands, `disqualifier_reason` not schema-constrained; **gates Sprint 4's hot-alert work and should be scheduled first**, since a false-positive disqualification silently suppresses the hot alert), #36 (Intelligence Scorer asserts company facts absent from the lead data, in two live instances — fabricated headcounts and an invented second company entity), #37 (GitHub Projects board auto-add is inconsistent again — not a repeat of the 2026-07-17 root cause, since the repo-project link and `Auto-add to project` are both confirmed still fine, but `Item added to project` has silently never been on since that fix, and one issue this session didn't auto-add at all for an unconfirmed reason). Also resolved mid-verification: FlowSignal's ICP scope is now explicitly B2B SaaS/tech (not any B2B company) — see docs/DECISIONS.md 2026-07-23 — and the per-lead cost claim is corrected from #29's single sparse-lead data point ($0.007) to a real 33-lead distribution (median $0.0095, worst observed $0.015, 100% under $0.02/lead).

Sprint 4 — CLOSED (2026-08-03; sprint ran Jul 27–31, closed administratively three days after the calendar boundary). Goal (closed loop form → ≤90s → HubSpot + hot alert) PARTIALLY met — the delivery half shipped and the alert half was never started. #40 was built and live-verified on its named day (Wed Jul 29): a form submission now flows form → enriched → scored → `icp_score` + grounded `company_summary` on the HubSpot contact, draft email attached as a note. But #41 was never begun, so no hot alert fires; and because #42 never ran, the ≤90s end-to-end timing has still never been measured. 1 of 6 committed issues closed. Work stopped after Jul 29; Jul 30–31 produced nothing. Full account, including the three findings and the one change: RETROS.md, "Sprint 4 Retro — 2026-08-03".

#38: grounding implementation deployed and verified live through the real webhook. `config/icp.default.json` bumped to `1.1.0-v1` (`hard_disqualifiers` as `{id, label, definition}` objects; B2B SaaS/tech scope stated explicitly as a scoring signal, never a gate). `prompts/lead-scoring.v2.md` (v1 kept for rollback) adds a data-fence rule forbidding world-knowledge facts, single-entity + message-wins precedence for message-vs-enrichment conflicts (added mid-implementation once the Ignite Visibility fixture exposed the gap — its own enrichment says "700+" while its message says "~120"), unconditional drafting (policy now lives entirely in `Compute Score`'s existing tier gate), and evidence/reasoning generated before the disqualification verdict. Post-model validation constrains `disqualifier_id` to the configured enum and requires an exact-substring-grounded headcount for `company_too_large`. #36 (fabricated facts) is root-cause-fixed and live-verified — Ignite Visibility correctly grounds on its message's ~120 over its own enrichment's "700+", Aerotek/Grainger correctly avoid the size gate via absent headcount data, a constructed IBM synthetic case held message-wins even against a genuine "300,000+ employees" figure — but **stays OPEN, not closed**, per #38's own body framing #35/#36 as closing together once the full regression suite passes.

#35 (`obvious_competitor` over-triggering) does not close on this pass — the Sanity fixture (a headless CMS, explicitly listed as a non-competitor example in config) still disqualified in the majority of real regression samples under the grounded v2 prompt. Root-caused via a controlled name-scrub experiment on a throwaway harness: real name, n=10, 60% disqualified; identical enrichment/message with the name scrubbed to a neutral placeholder, n=6, 16.7% disqualified — a 3.6× reduction, confirming the failure is dominantly a pre-textual name-recognition prior (no prompt-wording fix reaches a mechanism that operates before the model reads the definition), with a smaller ~15–20% description-driven residual confirmed independent of any name signal. Fix path: #44 (config-maintained competitor list checked in code, promoted from "consider" to primary fix for the dominant effect) plus #43 (log-only reasoning-vs-definition consistency check, retains bounded value for the measured description-driven tail on companies not on any list). Neither implemented yet — both Backlog and unscheduled; Sprint 4 closed without picking either up (see the blocker note below). #45 (promote evidence-provenance validation from log-only to enforcing, from the original #38 spec) also filed to Backlog.

Also this session: #35 was found to have been prematurely auto-closed by an early docs-only commit whose message happened to contain closing keywords — reopened, board column corrected, process note added (docs/DECISIONS.md) against using closing keywords except in the actual closing commit/PR.

Full verification/experiment evidence: docs/DECISIONS.md's 2026-07-29 entries, docs/RUNBOOK.md's "Ground the Scorer (#38)" section, and the comment threads on #35/#36/#38/#44.

#40 closed 2026-07-29 (named day, met): HubSpot delivery built and live-verified. The original #38→#40 dependency was re-evaluated after #38's scope split from #35 — #40 proceeded because #38's grounding work (its real blocker, trustworthy scorer output before writing to a CRM) is deployed and verified; the remaining #35 competitor residual only under-delivers wrongly-disqualified leads, it can't corrupt a HubSpot write. New migration `20260729150000_add_hubspot_contact_id.sql` (`leads.hubspot_contact_id`, applied manually via the SQL Editor) persists the HubSpot contact id at intake so delivery never has to read HubSpot back (keeps the original #3 migration's "write-only downstream" principle intact); one write-back node after `HubSpot - Upsert Contact` covers both the fresh-insert and dedupe-update paths since they already converge upstream, live-confirmed on both. New HubSpot contact property `company_summary` (string/textarea) created alongside #4's `lead_source`/`icp_score`. `intelligence-scorer.json` gained `HubSpot - Set Score & Summary` (PATCH, tier-independent) → `Draft Email Present?` → `HubSpot - Create Note` (POST `/crm/v3/objects/notes`, `associationTypeId: 202` confirmed live against the portal's own associations-labels API) → `Supabase - Mark Delivered` (first real use of the `delivered` status value, reserved since #3). Delivery failures dead-letter separately from scoring failures and deliberately do NOT revert `status` back from `scored` — a scoring failure means "redo the Claude call," a delivery failure means "the score was fine, just retry the HubSpot write," and conflating the two would misrepresent already-trustworthy data as needing a redo; live-verified via a forced failure (corrupted `hubspot_contact_id` mid-flight) landing at `status: scored` with a Resend-delivered alert stating the distinction explicitly. Full happy path, dedupe path, and forced-failure path all verified through the real webhook, not the editor; all test leads/contacts/notes cleaned up after. See docs/DECISIONS.md's 2026-07-29 entries and docs/RUNBOOK.md's "HubSpot Delivery (#40)" section.

Rolled to Sprint 5 (milestone #5, due 2026-08-07) at Sprint 4's close: #34 (null-domain lead loss), #39 (spot-check re-run), #41 (hot-lead alerts), #42 (end-to-end verification + Review recording) — all four were committed to Sprint 4 and never started. Left attached to the now-closed milestone #4 pending planning re-triage: #35, #36, #38. Board Status was deliberately not changed for any of them — planning re-triages.

**#44 is the load-bearing blocker.** Nothing downstream of the competitor bug can complete until a config-maintained competitor list exists: #39 needs it (a fresh in-ICP batch is B2B SaaS/tech, which is precisely the population `obvious_competitor` over-disqualifies — that is exactly how #30's batch 1 failed, with zero tier variance); #35 cannot close without it (the name-recognition prior operates before the model reads the definition, so no prompt wording reaches it); and #36/#38 cannot close until #35 does, since #38's body frames all three as closing together. #44 is itself Backlog and unscheduled — scheduling it is the first real decision Sprint 5 planning has to make.

#34 closed 2026-08-03 (worked ahead of Sprint 5 planning, on direct request — the sprint is still unplanned). The three-way outcome was ratified in docs/DECISIONS.md (2026-08-03) *before* implementation, per the issue's own "needs ratification, not a unilateral code change" condition — and the ratified outcome deliberately **rejects** the issue body's proposed direction: `skipped` keeps blocking scoring exactly like `failed` (the gate is a data-sufficiency check; an empty enrichment record has no grounded evidence to score against), so all-skip leads still do **not** reach the Intelligence Scorer. What changed is only the alert: an all-skipped lead is a lead data-quality condition, not a pipeline failure, and no longer dead-letters. `Compute Overall Status` now emits `allSkipped` beside the unchanged `anyOk`, and a new `All Skipped?` IF on the false branch (after `Supabase - Record Duration Only`, so duration is recorded either way) routes all-skip → `No Op - All Skipped (No Alert)` and everything else → the existing `Fail Execution - All Enrichment Failed`. `allSkipped` is `every(status === 'skipped')`, so an absent/unexpected status still alerts. Component status enum confirmed as `{ok, partial, skipped, failed}` before touching the gate (`partial` is emitted only by `Website Scraper`). Live-verified through the real webhook on all three paths — AC 1 (no domain + no company → `raw`, no Resend send at all, no `icp_score`/`company_summary`/note on the HubSpot contact), AC 2 (no domain + company → `News RSS` `ok` → scored `hot` 77, delivered, $0.0106), plus a guard test confirming a genuine `website: failed` still fires exactly one alert. Live export diffed identical to the repo; 4 test leads, 4 HubSpot contacts and 1 note cleaned up after. See docs/RUNBOOK.md's "All-skip enrichment gate (#34)" section — including the testing gotcha that a direct `curl` to the intake webhook never derives `domain` from the email (that lives in `web/src/lib/lead.ts`, web-app-only), which silently converts an intended unreachable-domain test into an all-skip test.

Sprint 5 — "Loop hardening" (milestone #5, Aug 3–7) was planned since this file was last updated — that planning happened outside a Claude Code session, so this file's "not yet planned" line above was stale, not a signal to re-plan. Goal: competitor name-prior killed, the loop's remaining bug fixed, held-open chain closed clean. Firm commit order: #44 (list-only, load-bearing, first) → #35 (deterministic once #44 lands) → #36/#38 (held-open bookkeeping) → #41 (hot-lead alerts, slips to Sprint 6 if #44 overruns) → #42 (end-to-end verification + review recording). #39/#45/dashboard v1/error-alert formalization/README+diagram+cost+Loom+load-test deferred to Sprint 6.

#44 closed 2026-08-03 (worked same-day as scheduled, ahead of a planning session touching it — milestone already committed it first). Ratified list-only, not list-first/model-fallback (docs/DECISIONS.md 2026-08-03): the model gets no vote on `obvious_competitor` at all, for any company — the 2026-07-29 name-scrub experiment showed the over-triggering is a pre-textual name-recognition prior that fires before the model reads the definition, so a fallback-to-model-judgment path would keep the same failure mode for whatever isn't yet on the list. `config/icp.default.json` → `1.2.0-v1`: new `competitor_list` (Attio, Clearbit, Apollo, Clay, HubSpot, Outreach, Salesloft), `obvious_competitor` flagged `enforcement: "code_list"`. `intelligence-scorer.json` gained a `Check Competitor List` → `Is Competitor?` → `Competitor Disqualification` branch ahead of the Claude API call — a match sets `disqualified/obvious_competitor/icp_score:10` and skips the model call entirely (confirmed via `n8nEventLog.log` grouped by `executionId`, not inferred from cost); `Render Prompt` no longer renders `obvious_competitor` as a model option at all; `Compute Score` gained one added branch forcing `icp_score:10` for the code-list path (required since `HubSpot - Set Score & Summary` reads `$('Compute Score')` by node name), every other path's weighted aggregation untouched. Live-verified through the real webhook, 7-lead regression fixture (Attio/Clearbit → disqualified, no Claude call; Raycast/Superhuman/Framer/Sanity/Photoroom → scored normally) — **Sanity, the #35 failure case that disqualified 60% of the time under real-name model judgment, now scores clean every time**. Fresh export diffed identical to committed JSON. See docs/RUNBOOK.md's "Config-maintained competitor list (#44)" section and docs/DECISIONS.md 2026-08-03.

#35 closed 2026-08-03, no implementation — deterministically resolved by #44, as predicted. Its two findings: (1) the over-trigger, closed by #44's fixture re-run (Attio/Clearbit still disqualify with zero Claude calls; Raycast/Superhuman/Framer/Sanity/Photoroom all clear — Sanity now scoring clean every time instead of 60% disqualified); (2) off-config `disqualifier_reason`, already closed earlier by #38's enum-constrained validation. Board auto-set to Done on close — no manual fix needed this time.

#36 and #38 closed 2026-08-03, no new implementation — their fix was deployed and live-verified back in Sprint 4 (2026-07-28/29: data fence, single-entity + message-wins precedence, `observed_headcount` receipt validation; Ignite Visibility/Aerotek/Grainger/IBM-synthetic cases all verified), and both stayed open only because #38's own body frames #35/#36/#38 as closing together, blocked on #35's competitor axis. #44 didn't touch this fix (separate disqualifier, no model call involved for a competitor-list match), so no re-verification of the grounding logic itself was needed — closure just reflects #35 now being closed. Production state was reconfirmed live the same day before closing #38 (fresh Attio lead through the real webhook, `n8nEventLog.log` execution trace). Board auto-set both to Done on close.

The #35/#36/#38/#44 chain from Sprint 4/5 planning is now fully closed.

#41 closed 2026-08-04 (crossed midnight UTC mid-session): hot-lead alerts shipped. A lead scored `tier: hot` fires a Resend alert to Wop's inbox as a normal success-path node after `Supabase - Mark Delivered` — deliberately not through `settings.errorWorkflow`, since a hot lead is a success, not a failure. New chain: `Hot Lead?` (IF, reads Compute Score's tier, doesn't recompute) → `Render Hot Alert Email` (Code, builds subject/HTML incl. a HubSpot contact deep-link + draft-email excerpt) → `Resend - Send Hot Alert` (HTTP Request, `onError: continueErrorOutput` → `Fail Execution - Hot Alert Failed`, which explicitly does not revert `leads.status` — a failed alert is a notification failure only). `config/icp.default.json` → `1.3.0-v1`: new `company_identity.hubspot_portal_id`, required in `ICP Config Loader` validation, needed to build the contact link without hardcoding a tenant-specific value. Live-verified through the real webhook: a hot lead (BrightLoop Metrics, 93) fired exactly one Resend alert (confirmed via Resend's own send log, not an inbox check); a non-hot lead (Cedarfield Consulting, 14/discard) fired zero. Verification caught and fixed a real bug — the draft-email preview initially took the literal first line, which is always just "Hi there," — before closing. Fresh export diffed identical to committed JSON; `Fail Execution - Hot Alert Failed`'s `onError: continueErrorOutput` wiring confirmed live as the hook #42 will exercise for the forced-failure test. All test data (3 Supabase rows, 3 HubSpot contacts, 2 notes) cleaned up. See docs/RUNBOOK.md's "Hot-lead alerts (#41)" section.

Next: #42 (end-to-end verification + Sprint 5 review recording) — the last item in the Sprint 5 firm-commit order before the milestone's Aug 7 due date.
[UPDATE THIS EVERY SESSION]

## Don't do this
- Don't re-litigate the stack or architecture — surface concerns to Wop instead.
- Don't implement backlog ideas mid-sprint.
- Don't scrape LinkedIn or any auth-walled source.
- Don't hardcode tenant-specific values (ICP, names, thresholds, sender identity).
- Don't make schema changes without a migration file.
- Don't swallow errors. No empty catch blocks.
- Don't echo secret values into output — verify presence/shape only (e.g. pattern-match a prefix, check non-empty), never print or feed into a command that could leak it on error (curl -v, unvalidated var extraction).
- Don't end a session with board state that contradicts issue state — the board is the single source of truth only if it's true. Verify with `gh project item-list`, don't assume `gh issue close` or a labeled automation handled it.
[grows over time]
