2026-07-02 — Sprint 1 runs Jul 2–10 (first sprint absorbs setup).
2026-07-02 — Supabase = source of truth; HubSpot write-only downstream. Why: we control the Postgres schema and query it freely for dashboard/metrics; HubSpot Free's API limits and rigid data model make it a poor primary store. Also keeps the pipeline CRM-agnostic (Phase 2 client could use a different CRM — only the delivery layer changes).
2026-07-02 — Budget: ~€5/mo Hetzner + ~$10 domain + ~$5-15 Claude API total. Rejected Oracle free tier (reliability risk) and going domainless (breaks Resend + demo credibility). Cost target is $/lead measured, not $0 spend.
2026-07-02 — Domain: nicopxm.me via Porkbun (at-cost, free WHOIS privacy, DNS colocated). n8n at n8n.<yourdomain>; root domain reserved for intake form + Resend sender identity.
2026-07-02 — Considered Railway over Hetzner VPS. Rejected: usage pricing punishes always-on n8n, and self-hosted VPS ops is a deliberate portfolio asset (issue #2 runbook = interview material). Hetzner stands.
2026-07-03 — Considered compressing the 5-sprint plan after fast start; deferred to Sprint 2 retro — two setup days ≠ evidence. If S1+S2 both close early, re-cut S3–5.
2026-07-03 — Issue #4 built with a HubSpot Service Key instead of a legacy private app. Why: HubSpot's private-app creation flow now flags private apps as legacy (no new scopes/features) and steers new integrations to Service Keys, which cover our use case (scoped, system-to-system contact read/write, no webhooks needed). n8n's HubSpot node "App Token" auth already accepts Service Keys — same mechanism, renamed. Confirmed via HubSpot's own docs/changelog, not a re-litigation of the stack (CRM choice unchanged, only the credential type).
2026-07-03 — Issue #7 error alerting uses n8n's native Error Trigger + Error Workflow pattern (a second workflow, `Lead Intake - Error Alert`, referenced in Lead Intake's Settings), not in-workflow try/catch. Why: this marks the failing execution as genuinely failed (visible in n8n's executions list per the AC) while still emailing Wop, and keeps the alert path reusable for future workflows without duplicating error-handling nodes. Email sent via Resend HTTP call (existing stack vendor) using its default `onboarding@resend.dev` sender — no domain verification needed since it only sends to the account owner's own inbox, which is exactly this use case (Wop alerting himself). Formal/multi-channel alerting is Sprint 4, per the issue.
2026-07-03 — Issue #7 dead-letters on partial failure: if Supabase insert succeeds but the HubSpot contact create then fails, the workflow updates that lead row to `status=error` (Supabase's own dead-letter state, per docs/ARCHITECTURE.md #8) before failing the execution, so the lead isn't silently stuck at `raw` with no CRM record and no error path.
2026-07-07 — Issue #7's Caddyfile changed from a single unconditional `basicauth` block to a `route {}` with a path-matched exception for `/webhook/*` and `/webhook-test/*`. Why: Caddy's basic auth was gating the entire n8n domain, including the intake webhook — but a universal webhook must accept POSTs from any lead source (a form, a partner's Typeform, Calendly), none of which have (or should have) the n8n editor password. The webhook itself validates its own payload (issue #7's Validate Payload node), so the endpoint doesn't need to be secret; the editor/API still sit behind basic auth. Discovered mid-issue while curl-testing, not scope creep — required to satisfy #7's own acceptance criteria.
2026-07-07 — Issue #7's HubSpot `lead_source` property is set via a dedicated HTTP Request node (`PATCH /crm/v3/objects/contacts/{vid}`) instead of n8n's native HubSpot node "Contact Properties" picker. Why: that picker only lists a curated common-property set and never surfaced our custom `lead_source` property, confirmed still present and unarchived via a direct HubSpot API check. The HTTP Request node reuses the same HubSpot Service Key credential (via n8n's Predefined Credential Type option), so no new secret or auth mechanism was introduced.
2026-07-07 — Revises 2026-07-02 domain decision: root `nicopxm.me` is no longer reserved for the intake form — it's now held for a future personal portfolio homepage (job-hunt asset). The pipeline (issue #5's Next.js app, and issue #6's intake form) lives at the subdomain `leads.nicopxm.me` instead. Resend is unaffected (its sender verification is domain-level on `nicopxm.me`, not path/subdomain-specific).
2026-07-07 — Issue #6's intake form POSTs to a Next.js server-side API route (`web/src/app/api/leads/route.ts`), not directly to the n8n webhook from the browser. Why: keeps `LEAD_INTAKE_WEBHOOK_URL` out of the client bundle entirely — it never appears in browser dev tools/network tab/page source, so the webhook can't be found and spammed straight from the public site (n8n's own payload validation from #7 is defense in depth, not the only gate). Secondary benefit: avoids a cross-origin request from `leads.nicopxm.me` to `n8n.nicopxm.me`. Costs one extra network hop per submission — negligible against the <90s success metric.
2026-07-07 — No phone field: no downstream consumer, conversion cost, PII liability. Phase 2 can add optionally per client — additive change.
2026-07-13 — Issue #19 Sprint 2 scaffolding, no headless browser for scraping. Why: JS-only/SPA sites failing to render is an accepted partial-failure case (graceful degradation, docs/ARCHITECTURE.md #7), not a gap to engineer around — Playwright/headless would roughly triple infra footprint (browser binaries, memory, execution time per page) to rescue a minority of sites, blowing both the latency budget and cost discipline for marginal enrichment gain.
2026-07-13 — Issue #19 Sprint 2 scaffolding, tech-stack detection via HTML fingerprinting on already-scraped pages, not a paid API (BuiltWith/Wappalyzer-as-a-service). Why: fingerprinting (script tags, meta generators, header patterns) is free and reuses the website step's output with no extra requests, and is sufficient for the `tech_maturity` ICP signal (Sprint 3) — a paid per-lookup API would eat directly into the <$0.02/lead budget for a signal that doesn't need paid-tier accuracy.
2026-07-13 — Issue #19 Sprint 2 scaffolding, news via Google News RSS queried by quoted company name, not a paid news/search API. Why: free, no API key, no rate-limit budget to manage. Accepted tradeoff: a common company name can collide with unrelated stories, so news items are documented as weak evidence — the intelligence prompt (Sprint 3) must treat `news.items` as suggestive signal, not verified fact, and should not be asked to state news-derived claims with confidence.
2026-07-13 — Issue #20 robots.txt: enforced minimally, not policy-only. Why: stated compliance must match actual behavior — documenting a permissive posture while not checking robots.txt at all would be a claim we can't back up. The website sub-workflow fetches `/robots.txt` first (5s timeout, no retry), parses only the `User-agent: *` block's `Disallow` paths, and prefix-matches each of home/about/pricing (and their fallback paths) against them before fetching. Disallowed pages get `pages.<page>.status=skipped, reason=robots_disallowed` (see docs/ARCHITECTURE.md contract), not `failed` — we chose not to fetch, nothing errored. Deliberately out of scope: `Crawl-delay`, per-lead caching of robots.txt, full RFC 9309 semantics (wildcards/`$` anchors) — a missing, unreachable, or unparseable robots.txt is treated as permissive (absent = allowed) so this feature can never itself break enrichment. Revisit if scrape volume/scope grows beyond 3 low-traffic marketing pages per lead.
2026-07-13 — Issue #20 page text cap: 4,000 characters per page (~1,000 tokens each; three pages ≈ 3,000 tokens total), applied after stripping `<script>/<style>/<nav>` and remaining tags. Why: keeps the enrichment record's token footprint bounded and predictable ahead of Sprint 3's intelligence call, without needing to tune per-lead — round number, generous enough for real marketing copy, documented so Sprint 3 can budget against it exactly.
2026-07-13 — Issue #20 empty-content threshold raised from 40 to 80 characters mid-verification. Why: live-tested against `app.excalidraw.com` (a real client-rendered SPA) and found its no-JS shell renders exactly `"Excalidraw+ You need to enable JavaScript to run this app."` — 58 characters, just over the original 40-char cutoff, which would have wrongly counted a useless boilerplate message as `status: "ok"` usable content. 80 is still tiny next to real page content (every genuine marketing page tested during verification ran into the thousands of characters) but clears this specific real-world false positive. Also fixed mid-verification: the `Extract Text` and `Parse robots.txt` Code nodes originally read `$json.body`, assuming the HTTP Request node's "Full Response" option nests the payload there; live testing against `stripe.com` showed the actual field is `$json.data`. Both nodes now check `$json.body ?? $json.data` so the extraction works regardless of which shape a given n8n/HTTP-node version returns.
2026-07-14 — Issue #21 amends #20's `website` contract with a `raw_artifacts` slice (script/link hostnames, meta generator, response header names) captured per page at scrape time, before HTML is stripped to `pages.*.text`. Why: #21's fingerprinting needs `script src`/`meta`/`link` host data that `pages.*.text` deliberately discards, and #21's own AC bans re-fetching pages. Rejected: storing full raw HTML (10-50x the stripped-text size, reopens #20's token-budget decision for no real benefit — fingerprinting only ever needs hostnames/generator/header-names, never the full markup); chaining #20 and #21 within one execution and passing raw HTML through memory only (couples the two sub-workflows' call order, and a lead can't be re-fingerprinted later against new fingerprint rules without re-scraping). The artifacts slice keeps sub-workflows decoupled and replayable at the cost of a small, deliberate #20 amendment — see the comment on issue #20 for the pointer.
2026-07-14 — Issue #21 detects React only when loaded from a public CDN (unpkg/jsDelivr script path markers) — self-bundled React (the overwhelming majority of real-world sites, built via webpack/vite/Next.js) has no distinguishing marker in our fingerprint scope (no separable host, no reliable path convention, and inspecting bundle contents is out of scope for a "hundreds of bytes/page" artifact budget). Accepted as an honest limitation rather than adding heavier detection (e.g. downloading and grepping JS bundles) that would blow both the latency budget and the artifact-size discipline for a signal `tech_maturity` scoring (Sprint 3) can likely infer well enough from Next.js detection alone (Next.js implies React).
2026-07-14 — Issue #21's fingerprint rules (`config/tech_fingerprints.json`) are read by the n8n workflow via a read-only directory bind mount (`${CONFIG_DIR}:/config:ro` in `n8n/docker-compose.yml`, `CONFIG_DIR` new in `.env.example`) rather than embedded/copy-pasted into a Code node. Why: single source of truth — editing the repo file and redeploying is the whole update path, no n8n editor changes required, and an embedded copy would silently drift from the repo file the moment either one changed (exactly the kind of institutionalized drift this project's config-driven-everything rule exists to prevent). Mounts the **directory**, not the single `tech_fingerprints.json` file, per the inode-pinning gotcha already documented from #7 (a single-file bind mount serves stale content after `mv`-style replacement; a directory mount doesn't have this problem for files edited in place via git pull). The path is an env var (`CONFIG_DIR`), not a hardcoded relative path, because the exact VPS directory layout (where this repo is actually cloned relative to `n8n/docker-compose.yml`) isn't something this decision should assume — same pattern as `N8N_HOST` and friends. A config file that's missing/unreadable/malformed at execution time is treated as an operational failure (`tech_stack.status=failed, reason=config_unreadable`, error-alert fires) — unlike every other skip/failure reason in this contract, a broken mount is an infra problem we want to know about immediately, not a graceful degradation. This also sets the pattern Sprint 3's ICP config should likely follow (config/env, not embedded in a Claude prompt template).

2026-07-15 — Issue #22 parses the Google News RSS response with n8n's built-in XML node (`n8n-nodes-base.xml`, mode `xmlToJson`) instead of regex/string parsing in a Code node. Why: the #21 RUNBOOK gotcha found the Code-node sandbox doesn't reliably expose the global `URL` constructor; rather than risk a second sandbox surprise hand-rolling XML parsing, the built-in node (backed by `xml2js`, confirmed present in the running n8n image) does it natively. Bonus: its default input field name (`data`) matches the HTTP Request node's `fullResponse` body field, so no extra field-renaming step was needed — same `$json.data` convention #20 already documented.
2026-07-15 — Issue #22 news items carry `title/source/date/url` (matching the issue's stated fields) rather than reusing #20/#21's `published_at` naming — this is a deliberate one-off; the field is genuinely a publish date on an external article, not this pipeline's own timestamp convention, so `date` reads clearer here. Recorded so #23's orchestration code doesn't "fix" it to match unrelated conventions.
2026-07-15 — Issue #22 verification used a purpose-built temporary harness workflow (`Harness - Test News RSS`: webhook trigger → Execute Workflow node → News RSS), imported via `n8n import:workflow`/`publish:workflow` CLI and driven with real `curl` POSTs, rather than editing a temporary node into an already-triggerable existing workflow (the literal #21 RUNBOOK harness description). Why: no n8n editor/browser access was available in this session; the CLI (`import:workflow`, `publish:workflow`, `export:workflow`, `list:workflow`) fully substitutes for manual editor work and was used for every deploy/verify/revert cycle in this issue, including forcing and confirming two genuine failures (bad RSS host → graceful `fetch_failed`, no alert; bad Supabase table name → real execution failure → confirmed `Lead Intake Pipeline failure - News RSS` email via the Resend API's own send log, not assumed). The harness workflow was unpublished (not deleted — no CLI delete command exists) after use; its webhook now 404s. `docker exec n8n-n8n-1 n8n <cmd>` plus a `docker compose restart n8n` (required after every publish for changes to take effect) is now the documented non-interactive path for deploying/testing n8n workflow changes — see RUNBOOK.
2026-07-17 — Issue #23's `Enrichment Orchestrator` calls Website Scraper → Tech Stack Detector → News RSS **sequentially**, not in parallel. Why: Tech Stack Detector's `no_html` skip depends on `enrichment.website.raw_artifacts` already being written to Supabase by Website Scraper — parallelizing would race that dependency and make the skip/detect decision depend on write-order luck. News RSS has no such dependency, but the issue explicitly allows sequential for it too, and a single linear chain avoids a Merge node entirely (the n8n operational notes already document Merge/branch fragility from #21). Tradeoff accepted: total orchestration latency is the sum of all three steps rather than the max, but this is well within the <90s form-to-CRM budget's 45s enrichment allowance (measured 8.8s–10.1s in live verification).
2026-07-17 — Issue #23 reads "all enrichment failed" (the alert trigger) broadly as **none of the three components reached `ok`/`partial`**, not literally "all three `status === 'failed'`". Why: this matches #20's own "entirely unenrichable lead" framing better than the narrow reading — a lead that's a mix of `failed`+`skipped` with zero real data (e.g. website fetch genuinely fails AND tech_stack skips on `no_html` AND news skips on `no_company`) is exactly as unenrichable as one where all three literally say `failed`, and CLAUDE.md's no-silent-failures rule is better served by catching that case too. **Known consequence, accepted**: any lead submitted with neither `domain` nor `company` alerts on literally every such submission, since nothing ever runs to produce `ok`/`partial` — acceptable while every current lead source (the intake form's domain derivation, `curl-test`) always supplies at least a company name; would need a carve-out if a future source can legitimately submit both empty, but that's speculative and out of scope here.
2026-07-17 — Issue #23 added `leads.enrichment_duration_ms` (migration `20260717120000_add_enrichment_duration_ms.sql`) as a **persisted** column rather than relying on n8n's own execution-log timing. Why: Sprint 4's latency dashboard needs a queryable baseline across all leads via SQL, not per-execution numbers that scroll out of n8n's UI; the column is written on every orchestration run regardless of outcome (success or all-failed) so the baseline isn't survivorship-biased toward only the leads that fully enriched.
2026-07-17 — Issue #23's `Lead Intake` → `Enrichment Orchestrator` call is wired as a genuinely non-blocking, fire-and-forget branch: `Supabase - Insert Lead` fans out to both the existing HubSpot branch and the new orchestrator call, and the orchestrator's `Execute Workflow` node has `options.waitForSubWorkflow: false` set explicitly. Why: relying on graph topology alone (the orchestrator branch simply not reconnecting to `Respond 200 - OK`) would likely also work, since n8n's webhook response fires once its own upstream chain completes regardless of parallel branches — but this was verified directly against the running n8n image's `ExecuteWorkflow.node.js` source (`n8n-nodes-base@2.28.3`) rather than assumed, and setting the option explicitly removes any doubt at negligible cost. Measured webhook response time after wiring: 3.4s, confirming the enrichment branch (which can take up to ~45s) never blocks it.
2026-07-17 — Issue #23's `Enrichment Orchestrator` sets `onError: "continueRegularOutput"` and `alwaysOutputData: true` on all three of its `Execute Workflow` calls (Website Scraper, Tech Stack Detector, News RSS) — both found necessary via live testing, not applied speculatively. Why: without `onError`, a called sub-workflow's own genuine failure (e.g. Tech Stack Detector's `config_unreadable`, see #26) propagated up and killed the entire orchestrator chain, so later steps never ran and the failure double-alerted (once from the sub-workflow's own Error Workflow, once from the orchestrator's). Without `alwaysOutputData`, a called sub-workflow completing successfully but with zero output items (Tech Stack Detector's `Config Unreadable?` IF node has no connection wired for its normal/false branch, by design, since standalone nothing needs to run after it) silently truncates the orchestrator mid-chain with no error and no alert — a recurrence of the #21 RUNBOOK "zero output items" gotcha through a new door, caught live during alert-path verification when a poll for completion never resolved because the execution had already finished, incompletely. Both fixes are cheap and defensive; without them, a sub-workflow that behaves perfectly well standalone can silently break composition the moment it's called from a sequential orchestrator.
2026-07-17 — GitHub Projects board (#1) had zero repositories linked to it since setup, discovered while closing out #23 (issue #26 never auto-added; #24/#25 sat stale in `Sprint` after rolling to Backlog). Fixed by linking the repo via `linkProjectV2ToRepository`, verified live with a throwaway test issue that auto-added within seconds post-fix. Why this matters going forward: the board was silently not the source of truth it claimed to be — every prior appearance of an issue on the board was a manual add during scaffolding, never automation. CLAUDE.md's Definition of Done now requires a `gh project item-list` check as the last step of every issue, not an assumption that `gh issue close` or an automation handled the board.
2026-07-20 — Issue #26's fix (`Tech Stack Detector`'s config-file parsing) uses n8n's built-in **Extract From File** node instead of the two alternatives considered: rewiring `Enrichment Orchestrator`'s invocation from `Lead Intake` to a webhook-based fire-and-forget call (matching an n8n community-reported workaround for the same bug class), or leaving the broken behavior in place and only surfacing a real error message. Why: the root cause (`getBinaryDataBuffer` failing specifically when reached through a `waitForSubWorkflow:false` chain, not reproducible in any manual/standalone test — see docs/RUNBOOK.md's n8n operational notes) is a platform-level async/execution-lifecycle issue, not a flaw in our workflow logic, so the cheapest fix that stays inside the affected node's own boundary was preferred over reworking #23's fire-and-forget invocation pattern (a genuine architecture change affecting webhook auth/security surface, out of proportion to this bug and not attempted without a working local fix to compare it against first). Surfacing the error only, without fixing behavior, was rejected outright — `tech_stack` was failing on 100% of real leads since 2026-07-17, and CLAUDE.md's no-silent-failures rule doesn't distinguish "documented" failures from silent ones when the failure rate is total. Verified via 5 consecutive real leads through the actual webhook path (not editor tests) plus a full re-run of #21's original 3-site/no-html/config-unreadable/recovery verification — see docs/RUNBOOK.md.

2026-07-20 — Issue #24's dedupe design deliberately creates a Supabase-vs-HubSpot asymmetry in the job-change edge case (same email, different domain): Supabase gets a **second** `leads` row (the new domain doesn't match the old one, so it's a new buying event per ARCHITECTURE #6), but HubSpot still has only **one** contact, since its native upsert matches by email alone — the second submission just overwrites that single contact's company-related properties with the new company's values. Accepted, not a bug: a `leads` row models a buying event (one per company engagement), a HubSpot contact models a person (one per person, full stop) — a person changing jobs is still one person, so HubSpot correctly collapsing to one contact matches how it models identity; Supabase, as the source of truth, retains the full per-company history via separate rows, which is what the dashboard/reporting layer needs. Revisit only if a future requirement needs HubSpot to track a contact's engagement per company distinctly (would need HubSpot's Company object + associations, not attempted here — out of scope for Phase 1). Stance taken proactively during planning, ahead of live verification, per Wop's direction not to leave this emergent. **Verified live 2026-07-21**: submitting `dedupe-test-24@example.com` first with `domain=dedupetest24.example.com`/`company=Dedupe Test Co`, then with `domain=differentcompany24.example.com`/`company=New Co`, produced two distinct `leads` rows (ids `b9d667f7-...` and `5852c105-...`, both reaching `status=enriched` independently) but exactly one HubSpot contact throughout (`id=236490934086`, `createdate` unchanged from the first submission) — its `company` property was simply overwritten to `New Co` by the second upsert. Matches the description above exactly; no follow-up needed.

2026-07-21 — Issue #28's `config/icp.default.json` values (tenant `flowsignal`: scoring dimension weights, thresholds, hard_disqualifiers, email_rules) are a v1 hypothesis, not validated against real conversion data — there's no scored-lead history yet to tune against, since #29 (the intelligence workflow that actually computes scores) hasn't landed. `config_version` carries a `-v1` suffix specifically so any score computed under these weights is traceable back to "pre-tuning" if they change later. The `company_fit`/`pain_signals` 30/30 split is the single biggest guess in the rubric (equal weight to "can they buy" and "are they in pain" — arguable either should dominate) and is the first thing to revisit once #30's human spot-check (10+ scored leads, tier agreement review) produces real signal. Until then, treat any hot/review/nurture tier assignment as provisional.

2026-07-21 — The VPS's repo clone (`/home/deploy/lead-intelligence-pipeline`) is a **deploy-time dependency that must be explicitly pulled, never assumed current** — it does not auto-sync from GitHub. Discovered during #28's deploy: the clone was stale since roughly #21, so the `CONFIG_DIR` bind mount served no `icp.default.json` at all (file didn't exist on the VPS's disk) despite being committed and pushed. A `git pull` on the VPS itself was required before the mount reflected reality. This is the same drift class as #7's Caddyfile inode-pinning gotcha and #26's fire-and-forget regression: the thing actually running on the VPS silently stops matching what's committed in the repo, and nothing surfaces that gap on its own — it's only ever caught by someone happening to look. Checked #28's other mounted files (`n8n/Caddyfile`, `config/tech_fingerprints.json`) and both matched cleanly this time, but that's incidental (nothing had touched them since #26), not a property of the setup. RUNBOOK.md's "n8n operational notes" now documents the pull as a required first step before any deploy touching a repo-mounted file. Scripting this so it can't be forgotten is deliberately not done here — see the new backlog issue for automating the deploy path; this entry only fixes the *documented* process, not the tooling.

2026-07-21 — Issue #28's `company_fit` dimension hard-caps company size at ≤200 employees (with `hard_disqualifiers` rejecting >1000 outright) rather than widening the gate to include larger companies. Why: FlowSignal's actual pain thesis is "no dedicated RevOps function yet" — companies in the 300–500+ range typically already have one, so admitting them would score companies as good-fit that don't actually have the problem FlowSignal solves, diluting the rubric's precision rather than its recall. This is a deliberate enactment of the project's Phase 2 thesis (CLAUDE.md: "EVERYTHING is config-driven, never hardcoded") — scaling to serve enterprise accounts later means shipping a *new tenant config file* (e.g. `config/icp.enterprise-client.json` with different weights/thresholds/ideal ranges), not loosening `icp.default.json`'s gate to try to serve both segments with one rubric. Keeping the default tenant's scope narrow and correct is what makes that swap-in-a-config-file story true instead of aspirational.

2026-07-22 — #29 intelligence layer: model returns score + reasoning + EVIDENCE per dimension, not score alone. Why: every score is auditable against the specific enrichment facts cited; an evidence array referencing facts absent from the lead data flags hallucination; and #30's human tier-agreement spot-check is only possible with visible reasoning, not a bare number. Costs a modest number of output tokens, buys a debuggable, defensible scorer.

2026-07-22 — #29: one structured call, draft-always, nurture-gate-governs-delivery-not-generation (option A). #29's AC specified "no draft generated below threshold (saves tokens)," which assumes the score is known before drafting — impossible in the one-call design (the model scores AND drafts in a single response; code sees the score only afterward). Honoring the AC literally would require a second call, breaking the one-structured-call architecture defended since Sprint 1 for a sub-cent (Haiku-priced) token saving. Resolution: draft always generated; code discards/withholds the draft below the nurture threshold rather than delivering/persisting it. AC reconciled via issue comment (plan-vs-reality, same pattern as #4 Service Key and #24 dedupe matching). Requires ICP-CONFIG.md email_rules wording update: nurture gate = "don't deliver/store the draft for sub-nurture leads," not "don't generate."

2026-07-22 — #29: division of labor between model and code. The model does JUDGMENT (per-dimension scores, evidence, company summary, draft email); the code does ARITHMETIC and POLICY (weighted total, disqualifier cap, tier assignment from thresholds, nurture gate). The deterministic steps never touch the model — this is the reproducibility guarantee: same enrichment + same config version = same icp_score and tier, every time. prompt_version + config_version stored per score so any historical score is interpretable after the prompt or ICP changes.

2026-07-22 — #29: `Intelligence Scorer`'s call to `ICP Config Loader` has no `onError` override, unlike the three sub-workflow calls inside `Enrichment Orchestrator` (#23's `continueRegularOutput` fix). Why the asymmetry: `Enrichment Orchestrator`'s components each cover one of three independent enrichment signals — one failing still leaves two others potentially usable, so continuing and alerting only once (via the failed component's own Error Workflow) is correct. A broken ICP config has no such partial-success path — nothing about scoring is possible without it — so `Intelligence Scorer`'s own execution failing too, and alerting a second time via the same `Lead Intake - Error Alert` workflow, is accepted as a known, minor cost rather than engineered around with new dead-letter semantics for a global-config-broken case that isn't any individual lead's fault.

2026-07-22 — #29 live verification surfaced two findings outside this issue's scope, deliberately not fixed here: (1) leads submitted with a reserved/special-use domain TLD (`.test`, `.example`) dead-letter at the `Lead Intake` level itself — before enrichment ever runs — almost certainly a HubSpot contact-creation rejection of those TLDs; filed as a backlog issue since it's a `Lead Intake`/HubSpot concern, not an Intelligence Scorer one. (2) the model sometimes sets `disqualified: true` for a reason outside `config/icp.default.json`'s four literal `hard_disqualifiers` (e.g. "insufficient lead data" for a very sparse-enrichment lead) — the prompt only instructs "if ANY of these is clearly true," and schema validation doesn't constrain `disqualifier_reason` to the configured list, so this passes validation cleanly. Not treated as a bug (the reasoning is honest and evidence-grounded, and CLAUDE.md's sparse-input-honesty intent is arguably served by it), but flagged explicitly for #30's human tier-agreement spot-check to weigh in on.

2026-07-23 — Issue #34: `Enrichment Orchestrator`'s `anyOk` gate treats `skipped` identically to `failed`, so a lead with both `domain` and `company` absent gets zero enrichable signal on all three components, never reaches `enriched`, and — since #29 wired scoring to the `anyOk === true` branch — `Intelligence Scorer` never runs at all. This upgrades #23's documented-and-accepted "alerts on every such submission" consequence into actual lead loss, not just noisy alerting. Found during #30's verification (2026-07-22): a null-domain/free-mail test lead stayed at `status=raw` while a same-information-content batch (domain present but unscrapeable, `company` supplied) scored normally, since `company` alone let `News RSS` return `ok` with an empty item list, flipping `anyOk` true on an otherwise equally sparse lead. Filed as #34, not fixed here — needs a ratified three-way outcome (any `ok`/`partial` vs. any `failed` vs. all-`skipped`) before implementation, not a unilateral code change.

2026-07-23 — Issue #35: `hard_disqualifiers` are four bare English phrases with no explicit test/boundary and no schema constraint on `disqualifier_reason`, unlike `scoring_dimensions` (each carrying `key`/`weight`/`ideal`/`notes`). Found during #30's verification (2026-07-22): 5 of 11 real-brand test leads (Raycast, Superhuman, Framer, Sanity, Photoroom) were disqualified as "obvious competitor" despite no actual category overlap with FlowSignal, while 2 of 10 fictional-company test leads carried `disqualifier_reason` values outside the configured list entirely, passing schema validation cleanly. Filed as #35, not fixed here — proposed direction adds an explicit test definition per disqualifier plus a schema-level constraint on `disqualifier_reason`, scheduled early in Sprint 4 ahead of hot-alert work since a false-positive disqualification silently suppresses the hot-alert path.

2026-07-23 — ICP scope resolved as narrow (B2B SaaS/tech). `config/icp.default.json`'s `icp_description` says "FlowSignal sells to B2B companies" without specifying industry, while the Series A–B funding language and the `tech_maturity` dimension imply a tech buyer. #30's batch 3 (12 non-tech B2B leads — manufacturing, construction, staffing, accounting, logistics, distribution) surfaced the ambiguity: the model resolved it toward "SaaS/tech only" and disqualified 5 leads for being "not a B2B SaaS/product company," a reason absent from the four configured `hard_disqualifiers`. Decision: FlowSignal's ICP is B2B SaaS/tech companies specifically, not any B2B company. Rationale: the enrichment stack discriminates far better on tech companies — batch 1 (real tech brands) returned 4–5 news items on every lead and real tech-stack detections; batch 3 (non-tech) returned 0–3 news items and mostly WordPress/Google Tag Manager/nothing, leaving `tech_maturity` (15%) and `market_timing` (5%) largely non-discriminating. Narrow ICP also aligns the demo tenant with the GTM-tooling segment this project targets. Consequence: `icp_description` must state the tech/SaaS scope explicitly rather than leaving the model to infer it — the inference is what produced the off-config disqualifier. Broadening to non-tech verticals later is a new tenant config file, per the 2026-07-21 precedent on the ≤200 ceiling, not a loosening of `icp.default.json`.

2026-07-23 — Prompt-adherence drift is a bug, not acceptable honesty. #30's review of all 33 test leads (25 disqualified) found 5/25 strictly novel disqualifier reasons and 8/25 (32%) novel-or-mixed against the four configured `hard_disqualifiers`. Three distinct failure patterns: (1) scoring ideal treated as a hard gate — `company_fit`'s ideal band (20–200 employees) applied as a disqualifier, when the configured size disqualifier is >1000; model text cited "the 200-person hard ceiling," and the dimension's `notes` field ("≤200 is a deliberate SMB/lower-mid scope, not a soft preference") appears to leak into disqualifier logic; (2) "not B2B SaaS" invented as a disqualifier — 5 leads killed on a criterion that appears nowhere in the config, inferred from `icp_description`'s venture-funding language; (3) applied inconsistently, and the inconsistency is expensive — in the same batch and run, Marlin Steel (wire fabrication) and Englewood Construction (commercial contracting) scored hot at 76 and 81, while River Pools, Ignite Spot, Frontline Source, Anders CPA, and Behlen Country were disqualified as non-SaaS; same category, opposite treatment, and the two that passed produced hot alerts on out-of-scope leads — the most expensive error mode in the rubric. Decision: this is a bug, not the sparse-lead honesty flagged on 2026-07-22 — that earlier finding was benign (redundant flags on leads already scoring 2–4 on dimensions), this is not, since it changes outcomes on leads that would otherwise score well and fires in both directions. Fix per #35 (`{key, test}` objects with explicit boundaries plus schema-enum validation of `disqualifier_reason`).

2026-07-23 — 30/30 weight revisit deferred to Sprint 4. #28 recorded the `company_fit`/`pain_signals` 30/30 split as the rubric's biggest guess, to be revisited after #30's human spot-check. That revisit is deferred, not dropped — #30 produced no usable weight data. The disqualifier gate capped 7 of 12 batch-3 leads at 10, short-circuiting the weighted aggregation on the majority of the batch; only 3 leads reached full weighted scoring (Marlin Steel, Englewood Construction, Thogus Products — where human and pipeline totals agreed within 2–5 points). Batch 3 is additionally invalid for tuning under the narrow-ICP decision above: all 12 leads were non-tech and therefore off-ICP. Requirement before the revisit: a fresh batch of in-ICP (B2B SaaS/tech) leads run after the disqualifier fix lands, so weighted scores aren't pre-empted by the gate. Overall human/pipeline tier agreement on batch 3 was 5/12 (41.7%) and is recorded as not interpretable for weight tuning. Methodological caveat: batch 3 used real company names with fabricated message details, so the model's world knowledge conflicted with the synthetic lead data — some divergence may be world knowledge rather than model error. Future spot-check batches should use fictional company names with realistic details, or real companies with accurate details, never a mix.

2026-07-23 — Rubric produces a bimodal tier distribution; the review band may be doing no work. #30's batch 3 produced zero review-tier leads from both the human judge (4 hot, 0 review, 6 nurture, 2 discard) and the pipeline (2 hot, 0 review, 1 nurture, 9 discard) — two independent passes, same gap. Likely structural: `pain_signals` (30) and `buying_intent` (20) covary — a lead with real pain usually states a timeline, one without has neither — so 50% of the weight moves together, pushing leads to the poles. The review band spans 48–71, 24 points wide, and caught nothing. Flagged for the deferred weight revisit; not acted on now, since batch 3 was off-ICP under the narrow-scope decision and the sample is too small to conclude from.

2026-07-23 — Self-reported facts the enrichment record doesn't confirm are weak evidence. Established while judging #30's batch 3: two leads claimed a CRM they didn't demonstrably run — Ignite Visibility (claimed HubSpot; site scraped successfully, HubSpot not detected) and Englewood Construction (claimed a basic CRM; site scrape failed entirely, nothing checkable). A successful scrape that fails to find a claimed tool is closer to contradiction than absence, so it warrants lower confidence than simply-missing data. Rule adopted for human scoring and proposed for the prompt: score claimed-but-unverified tooling low, and never treat a lead's self-report as equivalent to a detected fact. Relates to the fabricated-headcount issue (#36) — the same gap, in the opposite direction: the rubric specifies what to score but never how to weigh evidence quality.

2026-07-23 — `tech_maturity` can float a disinterested lead out of discard. #30 batch 3, Ignite Spot Accounting: message explicitly stated "just browsing, not really looking for anything," pain and intent both scored ~10, yet the lead cleared the discard threshold at 30 because detected HubSpot contributed 13.5 weighted points. Without `tech_maturity` it lands at 17 — discard. Not treated as a bug (a real CRM genuinely is a positive signal), but recorded as a candidate for the deferred weight revisit: a 15-weight capability signal can currently outvote explicit disinterest.

2026-07-23 — #30's 35 test leads are retained in Supabase as regression fixtures, contrary to the cleanup precedent set by #24/#28/#29. Those issues deleted their test data because it had no downstream use. #30's does: batch 1 is #35's named regression fixture, batch 3 is #36's evidence, and both are the before-state for the deferred `company_fit`/`pain_signals` weight revisit. Re-creating them would cost ~$0.35 in Claude calls and would not reproduce identical scores. HubSpot contacts for all 37 were deleted (no analytical value, pollutes a CRM used for demos). Two of the original 37 were deleted from Supabase as well — the Humaans dry-run and Doppler recording leads, both submitted through the live form with `source: "website_form"` and therefore indistinguishable from genuine production leads by any source-based filter. Neither was a fixture. **Consequence for Sprint 4's dashboard**: `leads` permanently contains 35 non-production rows. All 35 share the `issue-30-` source prefix, so `source NOT LIKE 'issue-30-%'` excludes them today. But that is a string convention, not a guarantee — this audit found three different source formats across five sub-batches (`issue-30-spotcheck`, `issue-30-spotcheck-b2`/`-b3`, `issue-30-verification-nulldomain`/`-dedupe`), and the two form-submitted rows had to be deleted precisely because no convention could cover them. A future test lead submitted through the real form would reproduce the problem exactly. The durable fix is a dedicated `is_test` boolean column (migration required) set at intake, rather than relying on source string discipline. Decide when the dashboard issue is picked up; the prefix filter is acceptable as an interim measure only if the limitation is documented alongside it.

2026-07-27 — Sprint 4 scope cut: error-alert formalization deferred to Sprint 5 to make room for #34 (null-domain lead loss), and dashboard v1 deferred to Sprint 5 on a dependency. Why recorded here and not only in the milestone description: the milestone description is overwritten each Monday, so the reasoning for the cut would vanish at next planning — DECISIONS is append-only. Rationale: #34 is genuine lead loss (free-mail + no-company leads never score via the anyOk gate), so it came into Sprint 4; per the slip rule (scope grows → something moves), one item went out. Error-alert formalization was the safest cut — it's the only Sprint 4 candidate that already partly exists (the Sprint 1 Lead Intake - Error Alert pattern already fires on failures), so cutting it degrades polish, not the no-silent-failures guarantee; the consequence (new delivery/alert nodes must inherit the existing error path) is carried into #42's verification as a forced-failure test. Dashboard v1 is dependency-forced: it can't be built cleanly until an is_test column exists to exclude #30's 35 retained test rows from metrics (see 2026-07-23 test-data-retention entry), which is itself Sprint 5 work. Sprint 4 goal unchanged: closed loop form → ≤90s → HubSpot + hot alert.

2026-07-29 — #38 revises 2026-07-22's draft-always/nurture-gate entry: the model no longer nulls `draft_email` on disqualification at all; drafting is unconditional (prompt rule 5) and all discard policy lives in `Compute Score`'s existing tier gate. Rationale: nulling on disqualification was policy executed by the model, contradicting the model-judges/code-enforces division re-affirmed below; and given the measured ~30%+ over-disqualification rate found across #30/#35, a model-side null was silently destroying drafts for leads that shouldn't have been disqualified in the first place. Required no new code — `draft_email: deliverDraft ? parsed.draft_email : null` in `Compute Score` already discards correctly once the model always drafts.

2026-07-29 — #38: `hard_disqualifiers` restructured from bare strings to `{id, label, definition}` objects; `disqualifier_id` constrained to the configured id set by post-model validation (enforcing), not by prompt instruction alone — this is the mechanism that would have caught #30's 32% off-config disqualifier reasons. `icp_description` now states the B2B SaaS/tech scope explicitly as a scoring signal, not a gate, closing the inference gap that produced 2026-07-23's invented "not B2B SaaS" disqualifier. Scoring-dimension `notes` rewritten so the 20–200 employee range and SaaS/tech focus read as guidance, never a gate — closing 2026-07-23's "200-person hard ceiling" leak. `config_version` bumped to `1.1.0-v1` (the `-v1` suffix intentionally survives: weights are still unvalidated pending #39, per the 2026-07-22 `1.0.0-v1` precedent).

2026-07-29 — #38: size disqualification (`company_too_large`) now requires a headcount the model actually extracted from `<lead>` — `observed_headcount.value > 1000` AND a `source_quote` that is an exact substring of the rendered `<lead>` block. World-knowledge size inference is forbidden by prompt rule 1 (data fence). Verified live: Aerotek and W.W. Grainger no longer hit `company_too_large` (no headcount anywhere in their supplied enrichment/message) but still land in `discard` via low dimension scores (12 and 7) — same outcome as before, honest mechanism instead of a world-knowledge shortcut. This was named in advance as an expected, not-a-regression consequence and confirmed exactly as predicted.

2026-07-29 — #38: prompt output key order changed so evidence and reasoning for all five dimensions are generated before the disqualification verdict, which now comes last (prompt rule: "Do not decide disqualification before examining the evidence"). The v1 schema put `disqualified`/`disqualifier_reason` first, forcing a verdict before any reasoning existed. Confirmed the reordering is actually in force in the deployed prompt (checked the rendered Output block directly) before concluding it wasn't sufficient on its own — see the `obvious_competitor` finding below.

2026-07-29 — #38: evidence-provenance validation (fuzzy token-overlap between each `evidence[]` string and the supplied `<lead>` data) ships log-only, writing to `leads.intelligence.validation_warnings[]`, not enforcing. Provenance is fuzzy matching and will false-positive on legitimate paraphrase; enforcing on day one risked dead-lettering good leads, a worse failure than the one it prevents. Promotion to enforcing tracked in #45, once a real batch's false-positive rate is measured (same pattern as #39's planned spot-check for score weights).

2026-07-29 — #38: message-wins precedence added for first-person fact conflicts (revises the original spec's rule 2, mid-implementation). When the inbound message and scraped enrichment disagree on a self-descriptive fact (headcount, funding stage, tooling), the message is authoritative — the lead speaks for their own company in the present tense; scraped copy is of unknown age and may describe a parent org, franchise network, or different scope entirely. Surfaced by the Ignite Visibility fixture, whose own scraped about-page says "700+ marketing experts" while the message says "around 120 people" — both figures legitimately present in `<lead>`, so the data fence alone couldn't resolve which was authoritative. Verified live twice: Ignite Visibility itself (`observed_headcount: {value: 120, source_quote: "Around 120 people"}`, not disqualified) and a constructed synthetic case (ibm.com — real "300,000+ employees" in scraped enrichment — paired with a message stating "about 40 people": scored on 40, `company_too_large` did not fire). The synthetic case specifically stress-tests what the exact-substring check alone cannot: a genuine >1000 figure legitimately present in `<lead>` only fails to trigger the gate because of the message-wins prompt rule, not because of any validation code.

2026-07-29 — #35 does NOT close on the `obvious_competitor` axis; #38 reduced but did not eliminate the over-disqualification pattern it named. Live regression on the Sanity fixture (headless CMS, explicitly listed as a non-competitor example in the config) under the v2 grounded prompt: 3 of 4 independent real samples still disqualified as `obvious_competitor`, each time with reasoning that never actually engaged the definition's category terms (lead enrichment/scoring/routing/RevOps automation/CRM delivery) — instead stretching generic "automation/platform/operations" language the config explicitly rules insufficient. Added a log-only post-model check (disqualifier evidence/reasoning vs. the matched definition's positive clause, token-overlap heuristic) — verified it correctly discriminates synthetic Attio/Clearbit-shaped correct matches (silent) from a Sanity-shaped off-definition case (flagged). But it caught neither of the two additional real over-disqualifications gathered after deploying it: one sample didn't disqualify at all, and the other's reasoning happened to literally reuse "RevOps automation" from the definition while making an illegitimate stretch argument, defeating token overlap. Filed #43 (promote the check to enforcing once measured) and #44 (consider a config-maintained competitor list instead of pure model judgment for this specific disqualifier, given the demonstrated 75%-on-one-fixture rate). #35 stays open until the competitor axis is actually resolved, not log-only-mitigated.

2026-07-29 — Process note: #35 was prematurely auto-closed by commit `5d54450`, a docs-only commit (adding the #38 spec file) whose message contained "closes #35, closes #36." GitHub's issue-closing keyword parser closes on any push to the default branch containing that exact phrase, with no understanding of surrounding context or actual resolution state — it doesn't require a merged PR, just a matching commit message reaching `main`. Caught via the routine final board-state check (`gh project item-list`), not proactively — #35 silently showed `Done` on the board while genuinely unresolved. Reopened, board column corrected back to In Progress, finding documented on the issue itself. Going forward: closing keywords (`closes #N`, `fixes #N`) belong only in the commit or PR that actually completes and verifies an issue, never in an intermediate commit, even when quoting an issue's own title/scope for documentation purposes.

2026-07-29 — Methodological note: re-verifying a retained regression fixture (#30's batch 1/3 rows) requires resubmitting to a genuinely new row, not just resubmitting the same payload to the existing row. `leads.updated_at` auto-touches to `now()` on any UPDATE (a database-level trigger, confirmed by directly testing it), including a raw PATCH made purely to backdate the timestamp for testing purposes — so attempting to force `Lead Intake`'s 30-day dedupe reenrich window open by backdating `updated_at` silently fails (the trigger overwrites it before the webhook's dedupe lookup ever reads it), leaving a resubmission looking successful (HTTP 200, message appended) while actually skipping re-enrichment and re-scoring entirely. Worked around by temporarily renaming the retained row's `domain` to free the real domain for a fresh `insert`-path row (which always sets `reenrich: true`), then restoring the original row's domain and deleting the throwaway row after recording results. No code change implied — this is a fixture-reuse technique for future sessions, not a pipeline defect: production leads never intentionally backdate their own timestamp.

2026-07-29 — #35 root cause identified: `obvious_competitor` over-triggering is a name prior, not primarily a description problem. Controlled experiment on the Sanity fixture: took its exact real enrichment + message and ran it twice — once as-is, once with every occurrence of "Sanity"/"sanity.io" scrubbed to a neutral placeholder ("Acme Corp"/"acmecorp.io") throughout the scraped text, holding the description content byte-identical otherwise. Ran via a throwaway harness workflow (webhook → `Execute Workflow` → respond, same disposable pattern as #28's `Harness - Test ICP Config Loader`; deactivated and confirmed 404 after use) so both conditions could be re-run repeatedly against a frozen, non-re-scraped snapshot rather than confounding the comparison with real scraping variance. Control (real name, n=10 — 4 from earlier full-webhook regression runs plus 6 harness runs): **6/10 disqualified as `obvious_competitor` (60%)**. Treatment (scrubbed name, n=6, harness only): **1/6 disqualified (16.7%)** — a **3.6× reduction**. The one treatment disqualification was checked for name leakage in its reasoning text and had none; it cited the same "automate manual work" / "Content Operations" marketing language present in several real-name disqualifications, confirming a genuine, if smaller, **description-driven residual (~15–20%) from ops/automation vocabulary that persists even with zero name signal**.

Two conclusions follow. (1) The name-prior effect is the dominant driver and is structurally unfixable by prompt tuning — recognizing "Sanity" as a known tech brand and pattern-matching toward "competitor" regardless of the configured definition happens pre-textually, before any reasoning about the actual definition occurs; no amount of rewording `obvious_competitor`'s definition reaches a mechanism that operates upstream of reading it. This promotes #44 (a config-maintained competitor list checked in code) from "consider" to **the primary fix** for the dominant failure mode — a maintained list sidesteps model judgment entirely for known cases. (2) #43 (the log-only reasoning-vs-definition consistency check) retains **bounded, now-measured value**: for a company not on a maintained list, the ~15–20% description-driven residual this experiment isolated is exactly the risk band #43 targets — its ceiling is no longer assumed but measured directly against this fixture. #44 alone would not close that tail; #43 is not overkill, but its scope is now known to be the minority failure mode, not the majority one this session originally suspected.

This is write-up only — #44 is not implemented here; it remains scheduled Sprint 4 work, not started under this entry.
